# Industrial Audit Rules (The Vault)
process_recipes:
  p_hello:
    inputs:
      - field: domain.counter
        condition: min
        value: 0
        level: S  # Strict Crash if < 0 (Safety)
    outputs:
      - field: domain.counter
        condition: min
        value: 0
        level: A  # Alert if violation (Quality)
      - field: domain.data
        condition: exists
        value: true
        level: S
